# VideoLens Skill 配置文件模板
#
# 使用方法：
#   1. 复制本文件为 .env：cp .env.example .env
#   2. 填入下方实际值（不要使用尖括号占位符，直接写值）
#   3. install.sh / validate_env.sh / analyze.sh 会自动加载 .env
#   4. .env 已被 .gitignore 忽略，不会被提交到 git
#
# 安全提示：
#   - .env 文件包含 API 密钥，切勿提交到版本控制
#   - 切勿在公开对话、截图、日志中暴露 .env 内容
#   - 如怀疑密钥泄露，请立即在服务商后台撤销并重新生成

# === OpenAI 兼容 API 密钥（必需） ===
# 商汤 sensenova 用户在此填写 sensenova 的 API key
OPENAI_API_KEY=

# === API 端点（必需） ===
# 商汤用户：https://token.sensenova.cn/v1
# OpenAI 用户：https://api.openai.com/v1
# 其他兼容服务商请填写对应端点
OPENAI_BASE_URL=https://token.sensenova.cn/v1

# === 视觉模型名称（必需） ===
# 用于分析视频帧（画面识别、OCR 等）
# 商汤推荐：sensenova-6.7-flash-lite（免费）
VIDEOLENS_MODEL_VISION=sensenova-6.7-flash-lite

# === 综合模型名称（必需） ===
# 用于综合分析（生成 findings / recommendations / tasks）
# 商汤推荐：sensenova-6.7-flash-lite（免费）
VIDEOLENS_MODEL_SYNTHESIZE=sensenova-6.7-flash-lite

# === Whisper 模型目录（必需） ===
# 指向 faster-whisper 模型文件的直接父目录（绝对路径）
# 下载方法参考 skills/videolens/references/whisper-setup.md
#
# 模型尺寸选择：
#   tiny   (~75MB)  速度快，准确度一般，适合短视频
#   base   (~145MB) 速度较快，准确度较好，通用场景
#   small  (~480MB) 速度中等，准确度好，长视频推荐
#   medium (~1.5GB) 速度慢，准确度很好，专业转录
#   large  (~3GB)   速度最慢，准确度最好，最高需求
VIDEOLENS_WHISPER_DIR=/home/admin/.cache/whisper/tiny

# === 可选：视频捕获时长（秒） ===
# 用于 PostHog / Hotjar / Clarity 等 session-replay 源
# 默认 60 秒
# VIDEOLENS_CAPTURE_DURATION=60
